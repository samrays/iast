# ==============================================================================
# Aegis IAST Platform & Google Online Boutique Demo Requirements
# ==============================================================================
# Usage:
#   python -m venv .venv
#   source .venv/bin/activate       # On Linux/macOS
#   .venv\Scripts\activate          # On Windows
#   pip install --upgrade pip
#   pip install -r requirements.txt
# ==============================================================================

# --- Web Framework & ASGI Server ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0
starlette>=0.38.0

# --- Validation, Data Models & Configuration ---
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0
email-validator>=2.2.0

# --- Database & Persistence ---
sqlalchemy[asyncio]>=2.0.32,<3.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0

# --- Security, Cryptography & Authentication ---
argon2-cffi>=23.1.0
argon2-cffi-bindings>=21.2.0
pyjwt>=2.9.0
pyotp>=2.9.0
cryptography>=43.0.0

# --- Telemetry, Metrics & Structured Logging ---
structlog>=24.4.0
prometheus-client>=0.20.0
rich>=13.7.0

# --- Caching, Network & CLI ---
redis>=5.0.0
typer>=0.12.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
colorama>=0.4.6

# --- Monorepo In-Tree Packages (Editable Installation) ---
-e ./agents/runtime/python-agent
-e ./apps/api
-e ./apps/gateway
-e ./apps/worker
